### 以下内容由AI生成:

# CMI 整合包热更新方案(讨论稿)

> 写给团队成员和朋友们的说明 · 状态: **讨论中**
> 一句话版本: **不用服务器、不用域名、不花钱**, 让玩家"双击一下, 新内容直接进实例, 照常开游戏"。

---

## 一、我们要解决的问题

当前玩家更新整合包的痛点:

- 每次更新要重新下载 / 重装 / 重新导入, 很麻烦
- 我们买不起服务器和域名(学生)
- 国内裸连 GitHub 不稳定
- 自研 Mod(`dev.celestiacraft`)还没发布, 没有平台能替我们分发

**目标**: 玩家已有实例不动, 拉取新内容后直接在原实例启动游戏, 全程不用重装。

## 二、好消息: 我们手里已经有的东西

| 资产 | 说明 | 价值 |
|---|---|---|
| GitHub 仓库 CodeNameCIM2 | 免费, 已有 PR 协作、issue 反馈 | 免费的"主服务器" |
| `mods/.index/` 218 条元数据 | 每个 mod 的 CurseForge `project-id`/`file-id` + sha1 哈希 | 现成的"mod 锁文件" |
| 自研 mod 的 jar | CMI Core 等 6 个, 在 git 白名单里 | 自研 mod 本体已经在仓库 |
| 版本号机制 + UpdateLogs.md | CmiGlobal.modPackMainVersion | 发版纪律已有雏形 |
| 224 个 jar vs 218 条索引 | 差的 6 个正是自研 mod | 印证索引覆盖完整 |

## 三、核心思路: 一份清单, 缺啥补啥

三个关键事实:

1. **CurseForge 的 CDN 直链可以拼出来**(packwiz 生态的公开约定), 不需要 API key:
   `https://edge.forgecdn.net/files/{file-id前4位}/{file-id}/{文件名}`
   → 公开 mod 直接从 CF 下载, **完全绕过 GitHub**, 国内一般可直连。
2. **自研 mod + kubejs + config 需要我们自己分发** → 放到 Gitee 免费镜像仓库(国内快)。
3. **玩家只需要一份"目标清单"** `update.json`(每个文件的路径、sha1、来源), 脚本对比本地文件, 缺什么补什么。

架构图:

```
[作者端] 打 tag → GitHub Actions 自动:
   读 mods/.index 元数据 → 生成 update.json(全量目标清单)
   自研 mod / kubejs / config / resourcepacks → 打成镜像 zip
   → 发 GitHub Release → 同步到 Gitee 镜像仓库

[玩家端] 双击 更新.bat:
   拉 update.json(镜像链: Gitee → jsDelivr → GitHub, 自动切换)
   sha1 对比 → 只下载有变化的文件
     公开 mod → CF CDN 直链
     自研 mod / kubejs / config → Gitee
   执行删除清单 → 写版本号 → 打印更新日志
   → 直接开启动器, 完成
```

优点:

- 玩家落后 N 个版本也能一次追上(清单是"目标状态")
- 自研 mod 和 kubejs 改动天然全覆盖
- 不依赖 CF API key、不依赖 GitHub 裸连、不强制装新软件
- 以后自研 mod 正式版上了 CurseForge, 改一行来源即可切换

## 四、为什么不用别的方案

| 方案 | 问题 |
|---|---|
| 发布 CurseForge/Modrinth, 让启动器更新 | 要过审、发版慢、玩家被绑启动器; 自研 mod 发不了 |
| 完整 packwiz | 布局要改造、自研 mod 要单独托管 URL、玩家要装 bootstrap jar, 对国内不友好 |
| 游戏内更新 Mod | 开发成本高, 可以等 v2 再做 |
| 纯整包 zip 增量 | 每次更新所有人都要下几十 MB; 元数据驱动可以只下变动的几个 jar |

**选型结论: 元数据驱动的原地更新脚本** —— 复用现有 `.index`, 双击即更, 零成本。

## 五、玩家视角: 更新长什么样

1. 把 `更新.bat` 放进整合包的 `.minecraft` 目录(第一次装包时自带)
2. 双击运行
3. 脚本自动检测新版本 → 只下载有变化的文件(断点续传, 断了不重来)
4. 旧文件自动备份到 `backups\更新_日期`(出问题可回滚)
5. 打印本次更新内容(来自 UpdateLogs.md)
6. 关闭窗口, 照常点启动器 → 完事

**安全红线**: 脚本只碰 mods / config / kubejs / defaultconfigs / resourcepacks 等可替换目录, **绝不碰 saves(存档)、options.txt、服务器数据**。

## 六、团队分工: 我们要做什么

开发组(我来做):

- [ ] 玩家端 `更新.ps1` + `更新.bat`(多镜像切换、断点续传、sha1 校验、回滚)
- [ ] GitHub Actions: 打 tag 自动生成 update.json + 镜像 zip + 发 Release + 同步 Gitee
- [ ] CI 校验: 224 个 jar 必须和索引对齐, 防止漏登记新 mod

需要一位同学提供(10 分钟, 免费):

- [ ] 注册 Gitee 账号, 建一个空的**公开**镜像仓库, 生成 token
- [ ] (或者先不做 Gitee, 纯 GitHub + 代理兜底, 但国内体验差一些)

全员要遵守的纪律:

- [ ] **打 tag 即发版**, 版本号写在 CmiGlobal.modPackMainVersion
- [ ] 新增/更换 mod 必须登记进 mods/.index(有 CI 校验兜底)
- [ ] 每次发版写 UpdateLogs.md(已有习惯, 继续)

## 七、风险与兜底

| 风险 | 对策 |
|---|---|
| GitHub 裸连不稳 | manifest 和镜像包优先走 Gitee, GitHub 只做兜底 |
| Gitee 单文件约 100MB 上限 | 镜像包按目录拆小块; 偶尔大更新提前公告 |
| CF 直链万一失效 | 脚本支持切 CF API 兜底; 发现后我们改元数据 |
| 更新出错 | backups 自动备份, 一键回滚 |
| 个别玩家 CF 直连慢 | 脚本支持手动配置代理/镜像前缀 |

## 八、待确认的问题(大家聊聊)

1. 用 Gitee 做国内镜像, 还是先纯 GitHub 顶着?
2. 发布节奏: 一周一更? 还是有大改动才发版?(影响玩家更新频率)
3. 自研 mod 要等正式版再上 CurseForge, 还是先随镜像包分发(推荐先分发)?
4. 玩家是不是基本都是 Windows?(脚本 v1 只做 Windows)
5. 服务端包要不要也支持更新?(服务端要删客户端 mod, 机制上可以做成两套清单)

---

## 附: 技术细节(给感兴趣的同学)

CF 直链拼接示例:

```
file-id 7178761 → https://edge.forgecdn.net/files/7178/761/create-1.20.1-6.0.8.jar
```

现有元数据示例(`mods/.index/create.pw.toml`):

```toml
filename = 'create-1.20.1-6.0.8.jar'
[download]
hash = 'b13d912b9247a38d66d11598c121442585a1c1e9'
hash-format = 'sha1'
mode = 'metadata:curseforge'
[update.curseforge]
file-id = 7178761
project-id = 328085
```

update.json 结构草案:

```json
{
  "packVersion": "2.5.0",
  "gameVersion": "1.20.1",
  "files": [
    { "path": "mods/create-1.20.1-6.0.8.jar", "sha1": "...", "source": "curseforge", "fileId": 7178761 },
    { "path": "mods/CMI Core-1.3-1.20.1-all.jar", "sha1": "...", "source": "mirror", "url": "https://gitee.com/xxx/cmi-mirror/raw/main/mods/CMI%20Core-1.3-1.20.1-all.jar" }
  ],
  "delete": ["mods/old-mod.jar"],
  "changelog": "https://raw.githubusercontent.com/Eternal-Snowstorm/CodeNameCIM2/main/UpdateLogs.md"
}
```

---

以上是讨论稿, 有想法直接回复; 确认后我就开始搭。
